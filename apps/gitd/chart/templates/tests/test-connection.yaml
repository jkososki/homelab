apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitd.fullname" . }}-test-connection"
  labels:
    {{- include "gitd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
