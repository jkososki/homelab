apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitd.fullname" . }}
  labels:
    {{- include "gitd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitd.selectorLabels" . | nindent 4 }}
